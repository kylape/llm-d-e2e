# Copied from llm-d-conformance-manifests at eb44bcb.
# Keep this file synchronized with that pinned source revision.
apiVersion: serving.kserve.io/v1alpha2
kind: LLMInferenceService
metadata:
  name: single-gpu-smoke
spec:
  model:
    uri: hf://Qwen/Qwen3-0.6B
    name: Qwen/Qwen3-0.6B
  replicas: 1
  router:
    scheduler:
      template:
        imagePullSecrets:
          - name: rhai-pull-secret
        containers:
          - name: main
    route: {}
    gateway: {}
  template:
    imagePullSecrets:
      - name: rhai-pull-secret
    containers:
      - name: main
        resources:
          limits:
            cpu: "2"
            memory: 8Gi
            nvidia.com/gpu: "1"
          requests:
            cpu: "1"
            memory: 4Gi
            nvidia.com/gpu: "1"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
            scheme: HTTPS
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 30
